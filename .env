# Menentukan nama aplikasi yang akan muncul di antarmuka
APP_NAME=Laravel
# Menyetel lingkungan aplikasi (local, staging, production, dll.)
APP_ENV=local
# Key enkripsi aplikasi untuk keamanan sesi dan cookie
APP_KEY=base64:W2bhZUd6cMBVHJGOwOqkQflmyLal/rjh6uNSdTdSa1Y=
# Mengaktifkan mode debug agar error detail ditampilkan
APP_DEBUG=true
# URL dasar aplikasi ketika diakses
APP_URL=http://localhost

# Menentukan channel utama untuk logging
LOG_CHANNEL=stack
# Menetapkan channel untuk mencatat deprecated warning
LOG_DEPRECATIONS_CHANNEL=null
# Batas level log minimum yang dicatat
LOG_LEVEL=debug

# Memilih driver database yang dipakai (sqlite)
DB_CONNECTION=sqlite
# Absolut path ke file database SQLite untuk kestabilan di Windows
DB_DATABASE=C:/Users/Rifqi/OneDrive/Documents/Github/ini-sempro/database/database.sqlite
# Host database jika menggunakan server
DB_HOST=127.0.0.1
# Port koneksi database
DB_PORT=3306
# Username untuk autentikasi database
DB_USERNAME=
# Password untuk autentikasi database
DB_PASSWORD=

# Driver yang digunakan untuk broadcasting event
BROADCAST_DRIVER=log
# Driver untuk caching aplikasi
CACHE_DRIVER=file
# Disk default untuk penyimpanan filesystem
FILESYSTEM_DISK=local
# Driver antrian yang menjalankan job
QUEUE_CONNECTION=sync
# Driver untuk sesi pengguna
SESSION_DRIVER=file
# Lama sesi dalam menit
SESSION_LIFETIME=120

# Host server memcached jika digunakan
MEMCACHED_HOST=127.0.0.1

# Host server Redis
REDIS_HOST=127.0.0.1
# Password untuk koneksi Redis
REDIS_PASSWORD=null
# Port koneksi Redis
REDIS_PORT=6379

# Driver pengiriman email
MAIL_MAILER=smtp
# Host server mail yang digunakan
MAIL_HOST=mailpit
# Port server mail
MAIL_PORT=1025
# Username untuk autentikasi mail
MAIL_USERNAME=null
# Password untuk autentikasi mail
MAIL_PASSWORD=null
# Jenis enkripsi koneksi mail
MAIL_ENCRYPTION=null
# Alamat email pengirim default
MAIL_FROM_ADDRESS="hello@example.com"
# Nama pengirim default (menggunakan nama aplikasi)
MAIL_FROM_NAME="${APP_NAME}"

# ID akses AWS (kosong jika belum diatur)
AWS_ACCESS_KEY_ID=
# Secret key AWS (kosong jika belum diatur)
AWS_SECRET_ACCESS_KEY=
# Region default AWS yang digunakan
AWS_DEFAULT_REGION=us-east-1
# Nama bucket AWS S3 jika dipakai
AWS_BUCKET=
# Mengatur apakah endpoint S3 menggunakan path style
AWS_USE_PATH_STYLE_ENDPOINT=false

# ID aplikasi Pusher (kosong saat belum diisi)
PUSHER_APP_ID=
# Key aplikasi Pusher
PUSHER_APP_KEY=
# Secret aplikasi Pusher
PUSHER_APP_SECRET=
# Host khusus untuk layanan Pusher
PUSHER_HOST=
# Port koneksi Pusher
PUSHER_PORT=443
# Skema protokol yang dipakai Pusher
PUSHER_SCHEME=https
# Cluster Pusher yang dipakai
PUSHER_APP_CLUSTER=mt1

# Key Pusher untuk frontend Vite
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
# Host Pusher untuk frontend
VITE_PUSHER_HOST="${PUSHER_HOST}"
# Port Pusher untuk frontend
VITE_PUSHER_PORT="${PUSHER_PORT}"
# Skema Pusher untuk frontend
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
# Cluster Pusher untuk frontend
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Target pemasukan bulanan dashboard demo
DASHBOARD_MONTHLY_TARGET=5000000
# Nominal yang sudah terkumpul di dashboard demo
DASHBOARD_MONTHLY_COLLECTED=1000000


# Kunci server Midtrans untuk integrasi pembayaran
MIDTRANS_SERVER_KEY=SB-Mid-server-QuqvFNf-jrE-EgGTytuaGGWK
# Kunci client Midtrans untuk frontend
MIDTRANS_CLIENT_KEY=SB-Mid-client-xQDzBwhMjT1tkgqb
# Menentukan apakah memakai mode produksi Midtrans
MIDTRANS_IS_PRODUCTION=false
# Mengaktifkan sanitasi data Midtrans
MIDTRANS_IS_SANITIZED=true
# Mengaktifkan 3DS untuk keamanan kartu kredit
MIDTRANS_IS_3DS=true
